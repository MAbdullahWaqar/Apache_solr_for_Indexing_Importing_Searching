<!--
  Reference snippets to merge into the core's solrconfig.xml.

  Solr 9 ships with a sensible default solrconfig.xml. The pieces below are
  the project-specific additions that wire up:
    * an /select request handler tuned for the books schema (edismax, hl, facets)
    * a /suggest handler backed by an analyzing infix Suggester
    * a Schema-API-friendly update chain
-->

<!-- ============================================================
     /select  - main search handler
     ============================================================ -->
<requestHandler name="/select" class="solr.SearchHandler">
  <lst name="defaults">
    <str name="echoParams">explicit</str>
    <str name="defType">edismax</str>
    <str name="qf">title^5 author^3 tags^2 description^1 _text_^0.5</str>
    <str name="pf">title^10 description^2</str>
    <str name="mm">2&lt;-1 5&lt;75%</str>
    <str name="tie">0.1</str>
    <str name="q.alt">*:*</str>

    <int name="rows">10</int>

    <!-- Highlighting -->
    <str name="hl">on</str>
    <str name="hl.fl">title,description</str>
    <str name="hl.simple.pre">&lt;mark&gt;</str>
    <str name="hl.simple.post">&lt;/mark&gt;</str>
    <int name="hl.fragsize">220</int>

    <!-- Facets (the UI driver passes facet=true and selects which facets) -->
    <str name="facet.mincount">1</str>
    <str name="facet.limit">15</str>

    <!-- Field list returned to clients -->
    <str name="fl">id,title,author,genres,language,year,pages,rating,price,
                   in_stock,stock_count,publisher,isbn,pub_date,tags,description,score</str>
  </lst>
</requestHandler>

<!-- ============================================================
     /suggest  - autocomplete handler
     ============================================================ -->
<searchComponent name="suggest" class="solr.SuggestComponent">
  <lst name="suggester">
    <str name="name">titleSuggester</str>
    <str name="lookupImpl">AnalyzingInfixLookupFactory</str>
    <str name="dictionaryImpl">DocumentDictionaryFactory</str>
    <str name="field">title</str>
    <str name="weightField">rating</str>
    <str name="suggestAnalyzerFieldType">text_en</str>
    <str name="buildOnStartup">true</str>
    <str name="buildOnCommit">true</str>
  </lst>
  <lst name="suggester">
    <str name="name">authorSuggester</str>
    <str name="lookupImpl">AnalyzingInfixLookupFactory</str>
    <str name="dictionaryImpl">DocumentDictionaryFactory</str>
    <str name="field">author</str>
    <str name="weightField">rating</str>
    <str name="suggestAnalyzerFieldType">text_en</str>
    <str name="buildOnStartup">true</str>
    <str name="buildOnCommit">true</str>
  </lst>
</searchComponent>

<requestHandler name="/suggest" class="solr.SearchHandler" startup="lazy">
  <lst name="defaults">
    <str name="suggest">true</str>
    <str name="suggest.dictionary">titleSuggester</str>
    <str name="suggest.dictionary">authorSuggester</str>
    <str name="suggest.count">10</str>
    <str name="suggest.build">false</str>
  </lst>
  <arr name="components">
    <str>suggest</str>
  </arr>
</requestHandler>

<!-- ============================================================
     /spell  - "did you mean?" suggestions
     ============================================================ -->
<searchComponent name="spellcheck" class="solr.SpellCheckComponent">
  <str name="queryAnalyzerFieldType">text_en</str>
  <lst name="spellchecker">
    <str name="name">default</str>
    <str name="field">_text_</str>
    <str name="classname">solr.DirectSolrSpellChecker</str>
    <str name="distanceMeasure">internal</str>
    <float name="accuracy">0.5</float>
    <int name="maxEdits">2</int>
    <int name="minPrefix">1</int>
    <int name="maxInspections">5</int>
    <int name="minQueryLength">3</int>
    <float name="maxQueryFrequency">0.01</float>
  </lst>
</searchComponent>
